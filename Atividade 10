## Atividade 10

#include <stdio.h>
#include <stdlib.h>
#include <time.h>
int main() {
    char nomes[5][50];
    printf("Digite 5 nomes:\n");
    for(int i = 0; i < 5; i++) {
        printf("Nome %d: ", i + 1);
        scanf("%s", nomes[i]);
    }
    printf("Nomes digitados:\n");
    for(int i = 0; i < 5; i++) {
        printf("%s\n", nomes[i]);
    }
    getch();
}
