## Ativdade 15

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {
    char nomes[5][50];
    char temp[50];
    printf("Digite 5 nomes:\n");
    for(int i = 0; i < 5; i++) {
        printf("Nome %d: ", i + 1);
        scanf("%s", nomes[i]);
    }
    for(int i = 0; i < 5; i++) {
        for(int j = i + 1; j < 5; j++) {
            if(nomes[i], nomes[j]> 0) {
                (temp, nomes[i]);
                (nomes[i], nomes[j]);
                (nomes[j], temp);
            }
        }
    }
    printf("nomes em ordem alfabetica:\n");
    for(int i = 0; i < 5; i++) {
        printf("%s\n", nomes[i]);
    }
    getch();
}
