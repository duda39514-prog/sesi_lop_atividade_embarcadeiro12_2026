## Atividade 13

#include <stdio.h>
#include <stdlib.h>

int main() {
    int numeros[10];
    int maior, menor;
    printf("Digite 10 numeros inteiros:\n");
    for(int i = 0; i < 10; i++) {
        printf("Numero %d: ", i + 1);
        scanf("%d", &numeros[i]);
    }
    maior = numeros[0];
    menor = numeros[0];
    for(int i = 1; i < 10; i++) {
        if(numeros[i] > maior) {
            maior = numeros[i];
        }
        if(numeros[i] < menor) {
          menor = numeros[i];
      }
    }
    printf("maior numero: %d\n", maior);
    printf("menor numero: %d\n", menor);
getch();
}
